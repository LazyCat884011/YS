<Window x:Class="WpfApplication2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication2"
        mc:Ignorable="d"
        Title="MainWindow" Height="384.028" Width="307.06">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <!-- To-do -->
    <StackPanel Grid.Row="1">
        <StackPanel x:Name="TodoList"></StackPanel>
        <Button x:Name="AddBtn" Click="AddBtn_Click">Add</Button>
        
    </StackPanel>
    <!-- Button -->
    <StackPanel Grid.Row ="2" Orientation="Horizontal">
            <Button Width="150" Name="SaveBtn" Click="SaveBtn_Click">Save</Button>
            <Button Width="150">Tasks</Button>
    </StackPanel>
    </Grid>
</Window>
